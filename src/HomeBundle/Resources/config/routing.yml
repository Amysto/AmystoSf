home_homepage:
    path:     /
    defaults: { _controller: HomeBundle:Default:index }
    options:
        expose: true

map_page:
    path: /map
    defaults: { _controller: HomeBundle:Default:map }